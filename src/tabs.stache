<ul class:from="this.tabsClass">
  {{# for(panel of this.panels) }}
    <li {{#if(this.isActive(panel))}}class='active'{{/if}}
			on:click="this.makeActive(panel)">
		  <a href="javascript://">{{panel.title}}</a>
	</li>
  {{/ for }}
</ul>
{{bitPanels()}}
